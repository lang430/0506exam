# 发现记录

## 项目结构
- 根目录当前只有考试说明 HTML、规划文件和 `demos/` 样例目录，尚无 Next.js 项目源码。

## 考试要求
- 技术栈要求：Next.js App Router + TypeScript。
- 核心功能：上传 Excel/Word/PDF，手动选择规则或新建规则，AI 先生成可编辑解析规则，用户确认后执行解析。
- 核心考点：通用规则描述和规则引擎，不允许按文件名或样例格式写专用 if-else。
- UI 要求：主色 `#0fc6c2`，清爽蓝绿色、圆角卡片、进度条、Toast、空状态、响应式。
- 数据要求：预览表格可编辑、实时校验、全量错误展示、重复外部编码检测、导出 Excel、提交持久化、历史列表筛选分页。
- 性能要求：1000 单 10 秒内导入展示，1000+ 行渲染不卡顿。

## 数据文件
- 当前实际文件：5 个 Excel、1 个 PDF、1 个 `package.json`。
- 题面描述为 9 份出库单，但当前 `demos/` 缺少 Word、周配送计划、多单 PDF 等样例。
- 5 个 Excel 样例均已纳入验证脚本，分别覆盖尾部信息提取、多 Sheet 合并、跨行同单多 SKU、卡片式边界、SKU × 门店矩阵转置。
- PDF 文件已通过 `pdfjs-dist` 文本抽取和默认文本明细规则验证，当前 PDF 解析 41 行、0 校验错误。
- 规则持久化已从浏览器 localStorage 升级为服务端 API，并强制写入 Postgres `parse_rules` 表；无数据库配置时返回明确错误。
- 性能验证：规则引擎解析 1000 行标准数据约 26 ms；前端用 300 行分批渲染控制大列表压力。
- `.xls` 支持已由 `@e965/xlsx` 提供，安装后 `npm audit` 为 0 漏洞。
- Supabase 数据库已实际初始化：3 张业务表存在，RLS 已启用，规则 API 使用 database 模式。
- AIHUBMIX 已接入；当前免费模型输出未稳定遵守纯 JSON，服务端已容错并降级，避免业务流程中断。
- AI 配额表 `ai_usage_events` 已加入数据库脚本并实际创建；限流规则为每分钟 5 次、每天 500 次。
- 多模型兼容已完成；供应商 429、非 JSON 输出和本地配额不足都会自动降级。
- 重新核对题面后补齐导入/提交的当前处理条数、历史提交时间筛选、Enter/Tab 单元格移动。
- 需求覆盖脚本已覆盖主要评分点，当前失败项为空。

## 待确认
- 无 Vercel/数据库/API Key 凭据，部署和真实数据库集成只能预留配置入口，无法在本机直接完成在线 URL。
- 题面对“同一外部编码下多个 SKU 聚合”为订单和“同批次外部编码重复检测”存在冲突；当前实现把同一外部编码的不同 SKU 视为合法聚合，只将“外部编码 + SKU 编码”重复作为同批次错误，历史数据仍按外部编码重复拦截。

## V4 审计（2026-08-04）
- 新需求目标为 V2 下单主链路异步事件驱动与全链路可观测性，目标分数 80。
- 红线包括在线部署、20,000 SKU 脚本、10,000 行 Excel、禁止同步逐行写入、禁止绕过规则引擎、禁止密钥入库。
- 核心高分项集中在可靠 Outbox、队列/Worker、批量校验和写入、处理单元幂等、行级错误、Trace/监控。
- 仓库当前为 Git `main`，已有 `.vercel`；工作树仅 `docs/V4-需求对照与评分梳理.md` 被修改、`.workbuddy/` 未跟踪，均视为用户现有内容并保留。
- 发现 Worker 强制重放时只阻止进度累计，错误明细和性能日志仍会在状态门闩前重复写入，不能满足完整处理单元幂等。
- 发现压测脚本原 PASS 判定未校验成功/失败行数、500/504 和服务端上传 P95，报告可能在验收数据错误时仍显示 PASS。
- 数据库集成测试会从全局队列认领其他残留任务，导致测试串扰；生产认领语义正确，但测试需定向目标任务。
- 依赖复核发现 Next 16.2.7、PostCSS 8.5.10 等安全公告；已升级到 Next 16.3.0、PostCSS 8.5.25，并执行 `npm audit fix`，当前审计为 0 vulnerabilities。
