# 万能导入 V2 提交说明

## 在线地址

当前本机开发地址：

```bash
http://127.0.0.1:3000
```

生产构建已通过 `npm run build`。Vercel CLI 已安装可用，但当前环境没有 Vercel 登录态、项目归属和数据库凭据，不能直接生成可公开访问的在线 URL。部署时可执行：

```bash
npx vercel
```

## 源码仓库

当前目录不是 Git 仓库，无法直接提供仓库链接。创建仓库后可将本目录推送到 GitHub / GitLab / Gitee。

## 大模型调用说明

规则生成入口位于 `/api/ai-rules`，流程如下：

1. 前端读取文件快照，仅截取前 30 行发送给服务端。
2. 服务端 Prompt 要求模型只生成 `ParseRule` JSON，不直接解析订单数据。
3. 模型输出必须包含 `assumptions`，标注推测项，用户需要人工确认后保存。
4. 已支持 `AIHUBMIX_API_KEY`、`DEEPSEEK_API_KEY` 和 `OPENAI_API_KEY` 环境变量。
5. 未配置 API Key 时，接口返回启发式规则草案，并明确标记为降级模式。
6. 当前已接入 AIHUBMIX，默认模型为 `coding-minimax-m3-free`。实测接口可连通，但该免费模型会输出非纯 JSON 内容；服务端已做 JSON 提取和失败降级，保证不会因为模型格式不稳定导致流程 500。

模型候选顺序可通过 `AI_MODELS` 配置，当前为：

```bash
xiaomi-mimo-v2.5-pro-free,xiaomi-mimo-v2.5-free,coding-glm-5.1-free,coding-minimax-m2.7-free,coding-minimax-m3-free
```

稳定性规则：

- 每分钟最多请求 5 次。
- 每天最多请求 500 次。
- 数据库可用时，限流记录写入 `ai_usage_events`，并用事务级 advisory lock 防止并发突破限制。
- 数据库不可用时，退回进程内存限流。
- 单个模型失败、供应商 429 或返回非规则 JSON 时，自动尝试下一个候选模型。
- 所有候选模型都失败时，返回启发式规则草案并标记 `degraded: true`。

可配置环境变量：

```bash
DEEPSEEK_API_KEY=你的密钥
OPENAI_API_KEY=你的密钥
AI_MODEL=deepseek-chat
DATABASE_URL=Neon 或 Supabase Postgres 连接串
```

## 数据库脚本

数据库初始化脚本位于 [database.sql](D:/AI/0605/database.sql)，可直接复制到 Supabase SQL Editor 执行。

脚本包含：

- `parse_rules`：解析规则持久化表。
- `import_batches`：导入批次表。
- `imported_orders`：已导入运单明细表。
- `ai_usage_events`：大模型请求计数表，用于每分钟和每日配额控制。
- 常用查询索引。
- `updated_at` 自动更新时间触发器。
- RLS 启用和 `anon/authenticated` 访问撤销。

本地已使用你提供的 Supabase Postgres 连接执行过脚本，确认 3 张表存在且 RLS 已启用。默认 6 条解析规则也已种入 `parse_rules`。

继续配置多模型后，已确认 `ai_usage_events` 表存在且 RLS 已启用。实测候选模型会按顺序尝试：部分免费模型返回供应商 429，部分模型返回非纯 JSON；系统会记录尝试、遵守本地限流，并降级为启发式规则，不影响导入流程。

## 已覆盖能力

- Next.js App Router + TypeScript。
- 文件上传入口支持 `.xlsx`、`.xls`、`.docx`、`.pdf`；Excel 读取使用审计通过的 `@e965/xlsx`。
- 规则管理支持选择、新建、编辑 JSON、保存、AI 草案生成和试解析。
- 规则保存到服务端持久化存储：配置 `DATABASE_URL` 时写入 Postgres；未配置时写入 `.data/rules.json`，不是只存在浏览器本地。
- 规则管理支持创建、编辑、删除、复制，上传文件时由用户手动选择规则，不做自动匹配。
- 规则引擎支持表格映射、尾部信息提取、多 Sheet 合并、卡片边界识别、矩阵转置、纯文本正则模式和 PDF 文本明细正则解析。
- 预览表格支持横向滚动、固定表头、单元格编辑、新增行、删除行、导出 Excel。
- 校验支持必填字段、A 组 / B 组二选一、电话格式、数量正数、同批次重复和历史重复。
- 提交接口支持 `DATABASE_URL` 写入 Postgres；无数据库时使用本地演示模式。
- 历史列表支持本地持久化、接口读取和筛选。
- 历史列表支持分页；预览表格按 300 行分批渲染，支持继续加载更多。

## Demo 验证

已通过脚本验证 5 个 Excel 文件和 1 个 PDF 文件：

```bash
npx tsx scripts/verify-demos.mjs
```

已通过性能脚本验证 1000 行标准数据：

```bash
npx tsx scripts/verify-performance.mjs
```

本机结果：解析 1000 行、0 个校验问题、约 26 ms，低于 10 秒要求。前端预览采用分批渲染，避免一次性渲染大表格导致卡顿。

验证结果：

| 文件 | 规则类型 | 解析行数 | 校验问题 |
|---|---:|---:|---:|
| `12.25海口龙湖天街-配送发货单PS2512220005001(1).xlsx` | 尾部信息提取 | 2 | 0 |
| `多门店分Sheet出库单.xlsx` | 多 Sheet 合并 | 21 | 0 |
| `湖南仓.xlsx` | 标准明细表 | 167 | 0 |
| `门店调拨单-卡片式.xlsx` | 卡片式边界 | 10 | 0 |
| `欢乐牧场模板0430.xlsx` | SKU × 门店矩阵转置 | 10 | 0 |
| `黔寨寨贵州烙锅（鞍山店）常温.pdf` | PDF 文本表格 | 41 | 0 |

题面描述为 9 份出库单，但当前 `demos/` 实际只有 5 个 Excel、1 个 PDF 和 `package.json`。缺少 Word、周配送计划、多单 PDF 等样例。当前已有 Word 纯文本入口和文本规则模式，但没有 Word 样例可做实测。

## 反思题

### 1. 规则应该描述到什么粒度？

规则应描述到「结构转换」粒度，而不是只描述字段名映射。原因是考试样例中存在尾部信息、跨行聚合、矩阵转置、卡片边界、多 Sheet 和纯文本拆分等结构差异。

太粗会让规则无法表达复杂格式，最后又退回硬编码。太细会让用户配置成本过高，AI 生成也更容易出错。本项目采用中间粒度：规则描述数据区域、字段映射、边界、尾部提取和矩阵列范围，具体执行交给规则引擎。

### 2. AI 生成规则和 AI 直接解析数据的优劣是什么？

AI 生成规则的优点是可复用、可审计、性能稳定，适合批量导入和反复使用的客户格式。缺点是首次配置需要用户确认，规则表达能力不足时需要扩展引擎。

AI 直接解析数据的优点是上手快，对临时文件友好。缺点是成本高、速度慢、结果不可控，且难以保证 1000 单 10 秒内展示。

本项目选择「AI 生成规则，规则引擎执行解析」。这是为了把不确定性限制在规则生成阶段，把批量解析变成确定性逻辑。

### 3. 纯人工编码需要多久？

如果不借助 AI，完成一个可演示版本大约需要 3 到 5 天：1 天搭建页面和导入流程，1 到 2 天设计规则引擎并覆盖 Excel 样例，1 天补 PDF / Word 和校验编辑，剩余时间处理部署、数据库和性能优化。

如果要求生产级稳定、完整兼容 9 类格式，并接入真实数据库和 Vercel，预计需要 1 到 2 周。
