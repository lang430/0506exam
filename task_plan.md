# 任务计划

## 目标
严格按照 `考试要求-文件版本.html` 的要求完成考试任务；`demos/` 目录中的文件作为导入样例使用。

## 成功标准
- 已读懂考试要求、导入文件格式和预期交互。
- 仅按要求实现必要功能，不额外扩展。
- 对关键细节做验证，并明确说明已验证项和未验证风险。

## 阶段
| 阶段 | 状态 | 内容 |
|---|---|---|
| 1 | complete | 熟悉项目文件与考试要求 |
| 2 | complete | 拆解任务与确认实现路径 |
| 3 | complete | 按要求完成必要实现 |
| 4 | complete | 导入 demos 样例并验证细节 |
| 5 | complete | 汇总结果与剩余风险 |
| 6 | complete | 按 V4 评分标准审计现有实现并估分 |
| 7 | complete | 选择达到 80 分所需的最小高价值缺口 |
| 8 | complete | 外科手术式实现并补齐行为测试 |
| 9 | complete | 全量验证、更新评分矩阵与交付说明 |

## 当前目标（V4）
- 对照 `exam-v4-v2-async-event-driven-observability.md` 逐项核对代码证据。
- 优先补齐异步事件驱动、批量性能、幂等恢复、错误明细、Trace/监控等核心评分项。
- 以可验证的 80 分为目标；在线部署和真实压测若受外部凭据限制，必须显式标记，不伪造结果。

## 当前成功标准
- 形成逐评分项的“已实现/部分实现/未实现/证据/预计分值”清单。
- 完成不依赖新增外部授权且对 80 分最关键的代码与测试。
- 运行 typecheck、自动化测试、build 及相关压测/数据脚本；失败必须记录。
- 线上全量压测仍需用户明确授权清理生产压测数据后执行。
- 依赖升级后安全审计恢复 0 vulnerabilities，构建/测试已复验。

## 遇到的错误
| 错误 | 尝试次数 | 处理 |
|---|---:|---|
| 仓库枚举命令首次超过 10 秒 | 1 | 拆分为更小的定向读取与检索，不重复原命令 |
| CodeRabbit CLI 未安装 | 1 | 不安装未知外部工具；改用本地静态审查、测试和运行证据 |
| Vitest 不支持 `--runInBand` | 1 | 使用仓库原生 `npm test`，不再传 Jest 参数 |
| 数据库集成测试认领到残留任务 | 1 | 认领函数增加可选 taskId，集成测试定向认领本场景任务 |

## 决策记录
- 采用单体 Next.js App Router 项目，前端完成文件解析、规则配置、预览编辑和本地历史持久化；服务端 API 负责 AI 规则生成入口。
- 无外部凭据时，AI 规则生成 API 返回启发式草案并明确标注推测字段；配置好 `OPENAI_API_KEY` 或 `DEEPSEEK_API_KEY` 后可走真实模型。
- 为安全审计清零，使用 `exceljs@4.4.0` 并通过 overrides 固定 `postcss` 和 `uuid` 补丁版本。
